---
category: "Core"
title: "InfoBar"
coverImage: "../sample-image.jpg"
slug: "Components/Navigation/InfoBar/code"
storybook: true
previewScale: 0.5
mainComponent: "InfoBar"
sampleCode: >
  <InfoBar>
    InfoBar with content. <Link href="#">Click here</Link> to do Something.
  </InfoBar>
---

### Usage

Import the `InfoBar` component:

```jsx
import InfoBar from "@wfp/react";
```

Here's a basic example of how to use the `InfoBar`:

```jsx
<InfoBar pageWidth="lg">This is important information.</InfoBar>
```

In this example, the `InfoBar` displays a message "This is important information." with an optional custom CSS class `custom-class`, an id `info-bar-id`, and sets the page width for the inner `Wrapper` component to `lg` (large).

### Notes

- The component has an integrated `<Wrapper />` to manage content width based on the screen size.
